# Supabase Configuration (required for Next.js)
NEXT_PUBLIC_SUPABASE_URL="https://your_project_id.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your_anon_key_here"

# Optional: API endpoints and feature flags
NEXT_PUBLIC_API_URL="http://localhost:3001"

# Instructions:
# 1. Copy this file to .env.local for local development
# 2. Copy this file to .env.production for production (Vercel)
# 3. Replace the placeholder values with your actual Supabase credentials
# 4. Note: In Next.js, only variables prefixed with NEXT_PUBLIC_ are available in the browser
# 3. Never commit the .env file to version control